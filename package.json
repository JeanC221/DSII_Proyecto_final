{
  "name": "DSII_Proyecto_final",
  "version": "1.0.0",
  "description": "Sistema de gestión de datos personales con consultas en lenguaje natural",
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose build",
    "stop": "docker-compose down",
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && npm install",
    "install-all": "npm run install-backend && npm run install-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "multer": "^1.4.5-lts.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JeanC221/DSII_Proyecto_final.git"
  },
  "keywords": [
    "react",
    "node",
    "firebase",
    "rag",
    "natural-language"
  ],
  "author": "",
  "license": "MIT"
}