version: "3.9"
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_started
    networks:
      app-network:
    restart: unless-stopped
    environment:
      - BACKEND_URL=http://backend:5000

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID:-default-project}
    networks:
      - app-network
    restart: unless-stopped
    volumes:
      - ./backend/src:/app/src
      - ./backend/src/credentials/firebase-credentials.json:/app/src/firebase-credentials.json:ro

networks:
  app-network:
    driver: bridge